"""Retired langstate research-benchmark placeholder.

The former project-specific proxy and every claim derived from it were
retracted. This file remains only so old imports fail loudly instead of
silently reproducing retired analysis. It contains no benchmark implementation
and is excluded from the published package by ``MANIFEST.in``.

No quantitative product or research claim survives through this module.
"""

raise ImportError(
    "langstate.te_check is retracted and no longer provides a working "
    "benchmark. See CHANGELOG.md v0.1.1."
)
