# Historical scrub note — superseded

This file preserves only the durable outcome of the July 2026 containment
work. It is not an execution runbook and none of its former publish, yank,
delete, or version commands should be used.

The historical pass removed private strategy fixtures and retired research
framing from the package payload, then constrained the source distribution to
the intended `src/langstate` package and metadata. Later releases and current
repository guidance supersede every version, branch, artifact, and command
that appeared in the original note.

For current work, use `README.md`, `CHANGELOG.md`, `pyproject.toml`, and the
repository's present tests. Any external action remains separately owner-gated.
